# -*- coding: utf-8 -*-
import re
import england as uk

text = uk.read_json_of_england()

matches = re.findall(r'=*.*=*', text)

for match in matches:
    i = 0
    while match[i] == '=':
        i++
    print(u'セクション名:' + match[i, -i])
    print(u'レベル' + (i - 1))
